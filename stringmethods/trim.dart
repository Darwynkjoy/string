import'dart:io';
void main(){
String name="   darwyn k joy    ";
String address="   palakkal";
print(name);
print(name.trim());//trim spaces on both side
print(address);
print(address.trimLeft());//trim spaces on left side
print(address.trimRight());//trim spaces on right side
}
